# ── CPU-only torch — installed via PyTorch's CPU index
# Using --index-url ensures pip never pulls CUDA builds (~2.5 GB)
# No version pin — pip picks the latest CPU build compatible with Python version
--index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# ── Core dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
chromadb==0.5.23
sentence-transformers==3.1.1
pydantic==2.9.2
psutil==5.9.8
ruff==0.4.9
