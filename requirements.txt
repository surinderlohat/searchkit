# ── Core dependencies
# NOTE: torch is installed separately in Dockerfile via PyTorch CPU index
# to avoid pulling the CUDA build (~2.5 GB)
fastapi==0.115.0
uvicorn[standard]==0.30.6
chromadb==0.5.23
sentence-transformers==3.1.1
pydantic==2.9.2
psutil==6.1.0
jinja2==3.1.4
python-multipart==0.0.9
ruff==0.4.9
