# ── CPU-only torch — must be installed BEFORE sentence-transformers
# Without this, pip pulls the default torch which includes CUDA (~2.5 GB)
# CPU-only version is ~200 MB instead
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.0+cpu
torchvision==0.18.0+cpu

# ── Core dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.6
chromadb==0.5.23
sentence-transformers==3.1.1
pydantic==2.9.2
psutil==5.9.8
ruff==0.4.9
